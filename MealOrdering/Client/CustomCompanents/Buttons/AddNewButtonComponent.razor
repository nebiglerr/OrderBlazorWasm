@inherits BaseButtonComponent


<ButtonComponent ButtonName="@(ButtonName ?? "Add")"
                 Color="@(Color ?? "btn btn-primary")"
                 Icon="@(Icon ?? "fa fa-plus-square")"
                 OnClick="OnClick"
                 ToolTipStr="@(ToolTipStr ?? "Add")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall" />