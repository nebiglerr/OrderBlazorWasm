
@inherits BaseButtonComponent


<ButtonComponent ButtonName="@(ButtonName ?? "Edit")"
                 Color="@(Color ?? "btn btn-outline-secondary")"
                 Icon="@(Icon ?? "fa fa-pencil-square-o")"
                 OnClick="OnClick"
                 ToolTipStr="@(ToolTipStr ?? "Edit")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall" />

