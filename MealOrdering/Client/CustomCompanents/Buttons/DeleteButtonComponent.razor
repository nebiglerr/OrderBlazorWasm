@inherits BaseButtonComponent


<ButtonComponent ButtonName="@(ButtonName ?? "Delete")"
                 Color="@(Color ?? "btn btn-danger")"
                 Icon="@(Icon ?? "fa fa-trash")"
                 OnClick="OnClick"
                 ToolTipStr="@(ToolTipStr ?? "Delete")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall" />
